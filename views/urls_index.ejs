<!DOCTYPE html>
<html>
<head>
  <% include ./partials/_head %>
</head>
<body>

  <header>
    <% include ./partials/_header %>
  </header>

  <main>
    <!-- List all URLs with an edit and delete button -->
    <ul>
      <% for (key in urls) { %>
        <li>
          <a href="localhost:8080/u/<%= key %>">localhost:8080/u/<strong><%= key %></strong></a> ----> <a href="<%= urls[key]['url'] %>"><%= urls[key]["url"] %></a> <%= urls[key]["dateCreated"].toString() %>
          <form class="urlItem" method="GET" action="./urls/<%= key %>"><input type="submit" value="Edit"></form>
          <form class="urlItem" method="POST" action="/urls/<%= key %>/delete"><input type="submit" value="Delete"></form>
        </li>
      <% }; %>
    </ul>
  </main>

  <footer>
    <% include ./partials/_footer %>
  </footer>

</body>
</html>